

pub fn fast_recurse_search(){
    // should take a path and query and only gets called with -f and there is a query string 
    // provided
    // also needs a progress bar to print
    println!("connected in seperate file");
}
